# With zstd-compressed zram, higher swappiness is beneficial —
# lets the kernel compress cold pages into zram more aggressively,
# freeing real RAM for active use. 150 is a good balance.
vm.swappiness = 150
# Bias toward reclaiming page cache over anonymous pages less aggressively
vm.page-cluster = 0
